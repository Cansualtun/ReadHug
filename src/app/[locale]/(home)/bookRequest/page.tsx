import React from "react";
import BookRequests from "../../components/bookRequest";

type Props = {};

const BookRequest = (props: Props) => {
    return <BookRequests />;
};

export default BookRequest;
